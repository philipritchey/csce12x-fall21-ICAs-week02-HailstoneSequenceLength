#include <iostream>
#include "code.h"

/*
f(n) = n/2 if n is even
f(n) = 3n+1 if n is odd
*/

int hailstone_sequence_length(int number) {
    // TODO(student): compute length of hailstone sequence
    return 1;
}